<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="300dp"
    android:layout_height="300dp"
    android:background="@drawable/img_rate_dashboard_background">

    <ImageView
        android:id="@+id/rate_dashboard_pointer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/img_rate_dashboard_pointer"
        android:scaleType="centerInside" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="73dp">
        <TextView
            android:id="@+id/rate_dashboard_step_per_min"
            android:layout_width="30dp"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:singleLine="true"
            android:ellipsize="end"
            android:textColor="@color/colorPrimaryDark"
            android:text="0"/>
        <TextView
            android:id="@+id/rate_dashboard_step_per_min_info"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorPrimaryDark"
            android:text="步/分钟"/>
    </LinearLayout>

</RelativeLayout>